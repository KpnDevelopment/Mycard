<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_background"
        android:label="@string/appname"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Settings_About_Btn"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".Settings_page" />
        <activity android:name=".View_page" />
        <activity android:name=".Update_page" />
        <activity android:name=".Filter_page" />
        <activity android:name=".Inserting_page" />
        <activity android:name=".Start_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>